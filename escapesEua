-- Carregar biblioteca
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

-- Aviso ao executar
Fluent:Notify({ Title = "Executado!", Content = "Executando com sucesso" })
Fluent:Notify({ Title = "Servidor XScripts oficial discord!", Content = "https://discord.gg/qsYR297DR2" })
Fluent:Notify({ Title = "Canal XScripts YouTube Oficial!", Content = "@XScriptsOficial" })
Fluent:Notify({ Title = "Este é Original Da XScripts!", Content = "Copyright © 2024 XScripts. Todos os direitos reservados.
" })

local Window = Fluent:CreateWindow({
    Title = "XScript escapes 🔥 | 🇺🇸 Versão 1.0.0",
    TabWidth = 160,
    Size = UDim2.fromOffset(390, 300),
    Theme = "dark"
})

local Tabs = {
    scripts = Window:AddTab({ Title = "escapes", Icon = "user" }),
}

-- Função para fechar a interface
local function closeInterface()
    Window:Destroy() -- ou Window.Visible = false, dependendo da sua implementação
end

-- Botões
Tabs.scripts:AddButton({
    Title = "Teacher's escape ", 
    Callback = function() 
        loadstring(game:HttpGet('https://raw.githubusercontent.com/HeyXmatheus/XScripts/refs/heads/main/escapeProf'))()
        closeInterface() -- Fecha a interface após executar o script
    end
})

Tabs.scripts:AddButton({
    Title = "Mr Funny Escape", 
    Callback = function() 
        loadstring(game:HttpGet('https://raw.githubusercontent.com/HeyXmatheus/XScripts/refs/heads/main/escapeSrFunni'))()
        closeInterface() -- Fecha a interface após executar o script
    end
})

Tabs.scripts:AddButton({
    Title = "Cowboy Escape", 
    Callback = function() 
        loadstring(game:HttpGet('https://raw.githubusercontent.com/HeyXmatheus/XScripts/refs/heads/main/escapeCowboy'))()
        closeInterface() -- Fecha a interface após executar o script
    end
})

Tabs.scripts:AddButton({
    Title = "Escape from the policeman Siren red", 
    Callback = function() 
        loadstring(game:HttpGet('https://raw.githubusercontent.com/HeyXmatheus/XScripts/refs/heads/main/escapeSrPolicial'))()
        closeInterface() -- Fecha a interface após executar o script
    end
})

Tabs.scripts:AddButton({
    Title = "Mr. Nightmare's Escape", 
    Callback = function() 
        loadstring(game:HttpGet('https://raw.githubusercontent.com/HeyXmatheus/XScripts/refs/heads/main/escapeSrPesadelo'))()
        closeInterface() -- Fecha a interface após executar o script
    end
})

Tabs.scripts:AddButton({
    Title = "Cook's Escape", 
    Callback = function() 
        loadstring(game:HttpGet('https://raw.githubusercontent.com/HeyXmatheus/XScripts/refs/heads/main/escapePizza'))()
        closeInterface() -- Fecha a interface após executar o script
    end 
})

-- Fim do código