-- Carregar biblioteca
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

-- Aviso ao executar
Fluent:Notify({ Title = "Executado!", Content = "Executando com sucesso" })
Fluent:Notify({ Title = "Servidor XScripts oficial discord!", Content = "https://discord.gg/qsYR297DR2" })

local Window = Fluent:CreateWindow({
    Title = "XScript universal 🔥 | Versão 1.0.0 ",
    TabWidth = 160, 
    Size = UDim2.fromOffset(580, 460), 
    Theme = "Amethyst"
})

local Tabs = {
    Tabinfo = Window:AddTab({ Title = "Tab Info" }),
    autoFarms = Window:AddTab({ Title = "Auto Farms", Icon = "farm" }), -- Renomeado para "Auto Farms"
}

-- Variáveis para controlar a coleta automática
local collectingCoins = false
local collectingHealingItems = false
local parasitarActive = false

-- Função para teletransportar moedas para o jogador
local function teleportCoins()
    local player = game.Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

    while collectingCoins do
        task.wait(0.1) -- Tempo entre as coletas

        for _, coin in pairs(workspace:GetChildren()) do
            if coin:IsA("Part") and coin.Name == "Coin" then -- Verifica se é uma moeda
                coin.Position = humanoidRootPart.Position -- Teleporta a moeda para a posição do jogador
            end
        end
    end
end

-- Função para teletransportar curativos para o jogador
local function teleportHealingItems()
    local player = game.Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

    while collectingHealingItems do
        task.wait(0.1) -- Tempo entre as coletas

        for _, item in pairs(workspace:GetChildren()) do
            if item:IsA("Part") and item.Name == "HealingItem" then -- Verifica se é um item de cura
                item.Position = humanoidRootPart.Position -- Teleporta o curativo para a posição do jogador
            end
        end
    end
end

-- Função para ativar/desativar parasitar
local function toggleParasitar()
    local player = game.Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()

    while parasitarActive do
        task.wait(0.1) -- Tempo entre as ativações

        -- Implementação do parasitar (a lógica exata depende de como o parasitar funciona no jogo)
        -- Aqui, você deve adicionar a lógica para fazer o parasita "pegar" os inimigos ou algo similar
    end
end

-- Alternador para ativar/desativar a coleta automática de moedas
Tabs.autoFarms:AddToggle("autoCollectCoins", 
{
    Title = "Auto Collect Coins", 
    Description = "Ativa ou desativa a coleta automática de moedas.",
    Default = false, 
    Callback = function(state)
        collectingCoins = state
        if collectingCoins then
            print("Coleta automática de moedas ativada")
            teleportCoins()
        else
            print("Coleta automática de moedas desativada")
        end
    end 
})

-- Alternador para ativar/desativar a coleta automática de curativos
Tabs.autoFarms:AddToggle("autoCollectHealingItems", 
{
    Title = "Auto Collect Healing Items", 
    Description = "Ativa ou desativa a coleta automática de curativos.",
    Default = false, 
    Callback = function(state)
        collectingHealingItems = state
        if collectingHealingItems then
            print("Coleta automática de curativos ativada")
            teleportHealingItems()
        else
            print("Coleta automática de curativos desativada")
        end
    end 
})

-- Alternador para ativar/desativar o parasitar
Tabs.autoFarms:AddToggle("autoParasitar", 
{
    Title = "Auto Parasitar", 
    Description = "Ativa ou desativa a habilidade de parasitar inimigos.",
    Default = false, 
    Callback = function(state)
        parasitarActive = state
        if parasitarActive then
            print("Auto Parasitar ativado")
            toggleParasitar()
        else
            print("Auto Parasitar desativado")
        end
    end 
})

-- Fim do código